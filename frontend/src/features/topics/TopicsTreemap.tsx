import type { ReactElement } from 'react';
import type { TopicsResponse } from '../../api/topics';

/**
 * 意圖主題 treemap (T3.4, FR-8; TC-19 圖表) — red stub (not yet implemented).
 */
export function TopicsTreemap(_props: { topics: TopicsResponse | undefined }): ReactElement {
  return <div />;
}
