import type { ReactElement } from 'react';
import type { TopicsResponse } from '../../api/topics';

/** RED stub (T3.3): not implemented — renders a placeholder so TC-19 表格 fails. */
export function TopicsTable(_props: { topics: TopicsResponse | undefined }): ReactElement {
  return <div>topics-table-todo</div>;
}
