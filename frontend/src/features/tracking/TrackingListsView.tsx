import type { ReactElement } from 'react';

/** TC-40 red stub — real CRUD view lands in the green commit. */
export function TrackingListsView(): ReactElement {
  return <section aria-label="追蹤清單管理" />;
}
