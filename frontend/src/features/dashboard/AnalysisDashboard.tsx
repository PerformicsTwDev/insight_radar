import type { ReactElement } from 'react';

/** STUB (T6.0 red) — real implementation lands in the green commit. */
export function AnalysisDashboard(_props: { analysisId: string }): ReactElement {
  return <div data-testid="analysis-dashboard-stub" />;
}
