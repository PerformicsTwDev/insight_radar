import type { ReactElement } from 'react';

/** STUB (T6.0 red) — real implementation lands in the green commit. */
export function ViewNotFound(_props: { view: string }): ReactElement {
  return <div />;
}

/** STUB (T6.0 red) — real implementation lands in the green commit. */
export function UnavailableView(_props: { view: string; label: string }): ReactElement {
  return <div />;
}

/** STUB (T6.0 red) — real implementation lands in the green commit. */
export function AnalysisNotFound(): ReactElement {
  return <div />;
}
