import type { ReactElement } from 'react';

/** STUB (T6.0 red) — real implementation lands in the green commit. */
export function KeywordsView(_props: { analysisId: string }): ReactElement {
  return <div />;
}
