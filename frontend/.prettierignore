# Generated / lock files — prettier must not reflow these.
# (pnpm-lock.yaml reflow breaks `pnpm install --frozen-lockfile`.)
dist
coverage
node_modules
pnpm-lock.yaml
*.local

# Playwright generated artifacts (baselines are PNG — no prettier parser anyway).
test-results
playwright-report
blob-report

# openapi-typescript 產物（勿 reflow；drift 由 openapi:check 對 raw 輸出比對）
src/api/schema.d.ts
