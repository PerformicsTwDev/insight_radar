# === 祕密 / env（NFR-5 / TC-29）===
# 本機 .env 由 .env.example 複製；.env.example 與 .env.test 為 committed（不 ignore）
.env
.env.local
.env.*.local
*.pem

# === Node / build ===
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# === 測試 / 工具 ===
.jest/
.eslintcache

# === 日誌 ===
logs/
*.log
npm-debug.log*

# === OS / IDE ===
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# === Claude Code / 專案文件 ===
# ⚠ 實際政策（ADR-0004，本機 only）：.claude/、CLAUDE.md、docs/_p/ 整批由「全域」
# ~/.gitignore_global 忽略、不入庫（以全域為權威來源）。下行僅為 defense-in-depth：
# 防全域規則缺失的機器意外提交個人覆寫檔。
.claude/settings.local.json
