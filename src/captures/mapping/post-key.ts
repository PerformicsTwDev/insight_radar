/**
 * postKey 正規化（S13 / AC-46.1；Design §18.5）——純函式。`normalize(permalink/url)` = 唯一去重鍵。
 *
 * TODO(T13.4): 實作。
 */
export function normalizePostKey(value: unknown): string | null {
  void value;
  return null;
}
